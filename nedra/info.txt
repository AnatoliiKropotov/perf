    Касаемо недочётов/опечаток в задании 1:
1.Считаю что в конструкции "Обобщённый вид арифметического выражения: [операция1](число)[(операция2)(число)]* ..."
не хватает ещё одного аргумента (числа) и она должна выглядеть так: [операция1](число)[(операция2)(число)]*[число]
2.В примерах выражений, пример номер 4 записан накорректно "- 6 * 2 → -11.98". Результатом данного выражения будет -12.
3.В доп задании "limit (int) - ограничить количество выводимых запросов; при значениях меньше 1 и больше 30, возвращается ошибка;"
не указано с начала или конца массива ограничивать запросы, решил ограничить с конца, по аналогии с сохранением в памяти последних 30 запросов.

В целом в задании 2 не успел сделать две дополнительные задачи:
-Возможность задать количество подряд идущих крестиков/ноликов для победы, например 5 для поля 20*20.
-Использование осмысленного алгоритма или иного способа игры компьютера, отличного от случайного проставления крестиков/ноликов.
Есть представление как их сделать (через сравнение соседних ячеек от последней заполненной), не хватило времени реализовать алгоритм.
